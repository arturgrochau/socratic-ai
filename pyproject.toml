[project]
name = "socratic-ai"
version = "2.3.0"
description = "Turn videos, PDFs, and lectures into a Socratic thinking workout — private by default: local models, local transcription, one-click API switch."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "nicegui",
    "httpx",
    "openai",
    "python-dotenv",
    "sqlalchemy",
    "chromadb",
    "pymupdf",
    "ffmpeg-python",
    "python-multipart",
    "yt-dlp",
    "platformdirs",
    "requests",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]
# On-device transcription (Apple Silicon / MLX). Local LLM+embedding roles only
# need a running Ollama daemon — no extra Python deps.
local = [
    "parakeet-mlx; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "mlx-whisper; sys_platform == 'darwin' and platform_machine == 'arm64'",
]

[project.scripts]
socratic-ai = "run:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app", "routes", "frontend", "prompts"]

[tool.hatch.build.targets.wheel.force-include]
"main.py" = "main.py"
"config.py" = "config.py"
"run.py" = "run.py"

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-p nicegui.testing.user_plugin"
markers = [
    "e2e: end-to-end pipeline tests (slow; require cassettes or OPENAI_API_KEY).",
]

