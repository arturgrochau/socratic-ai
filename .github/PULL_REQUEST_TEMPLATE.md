## What this changes

## Why

## Checks

- [ ] `uv run ruff check .` passes
- [ ] `uv run pytest tests/ -m "not e2e"` passes
- [ ] Prompt or schema changed: `GENERATION_SCHEMA_VERSION` bumped and the cassette re-recorded
- [ ] Design change: ADR added under `docs/adr/`
